
<h3>DEMANDE DE CONFIRMATION DE VOTRE NOUVEAU COMPTE SNOWTRICKS</h3>
  
<h4>" Cher {{ member.username|capitalize }}, cliquez sur ce lien pour confirmer votre compte Snow Tricks "</h4>

<a href="{{ url('confirm_account', { username : member.username , token : member.token }) }}" class="button">CONFIRMER MON COMPTE<a>

{#

path() - Generates a [relative] URL path given a route name and parameters.

url() - Generates an absolute URL given a route name and parameters.

<a href="{{ url('confirm_account', { username : member.username , token : member.token }) }}" class="button">VALIDER LE NOUVEAU COMPTE<a>

#}
